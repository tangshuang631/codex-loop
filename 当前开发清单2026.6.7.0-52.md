# 当前开发清单 2026.6.7 0:52

## 当前结论

- 仓库当前目标已经切换为纯通用的 `codex-loop` 发布形态，不再内置任何用户私有项目示例。
- 当前优先主线是两件事：
  - 核心循环链路稳定可用：创建、开始、续跑、停止、健康检查都能跑通。
  - 前端真正可用：白色极简、中文优先、左栏管理多 loop、长任务有明确动态反馈。
- 当前已确认：
  - 多 loop 与线程绑定链路可用
  - /api/mobile 已接通，可给移动端查看当前 loop 聊天镜像
  - Ollama 模型列表接口已接通
  - 启动检查已经回到通用模式，不再依赖任何固定示例项目

## 本次重点完成

- 去除仓库默认项目耦合
  - 通用默认 `config.json`
  - 删除本地覆盖里的固定项目路径
  - 默认 loop registry 改为纯通用模式
- 前端重构为白色极简工作台
  - 左侧：项目分组、loop 列表、创建助手、线程绑定
  - 右侧：仅显示当前 loop 的状态、聊天记录、设置、策略与健康检查
  - 删除主界面大段路径噪音
- 补上长任务动态反馈
  - 首屏骨架屏
  - 思考中三点动画
  - 轮询中状态提示
  - 页面失焦后自动放慢轮询，降低长时间运行卡顿概率
- 接通 Ollama 模型列表
  - GET /api/ollama/models
  - 前端可直接选择本地模型作为动态续发模型
- 核心续发 prompt 改为中文优先
  - 默认中文
  - 设置中可切英文
  - 动态生成关闭时走模板续发，开启时走本地 Ollama

## 当前可用闭环

- 创建 loop
- 选择 loop
- 绑定线程
- 开始循环
- 手动续跑一轮
- 停止并进入收尾
- 查看健康检查
- 查看最近续发与聊天镜像
- 手机端只读查看当前 loop 记录

## 仍需继续推进

### P0

- 继续加强真实线程续发体验，让自动续发更像真人接话
- 补更完整的最终收尾摘要收口
- 继续打磨移动端查看与基础控制
- 增强日志健康检查与异常提示

### P1

- 接入更完整的 NPC / 提示词生成策略
- 继续拉开与原生自动化的差异化：上下文卡片、节奏策略、暂停条件、Git 守护提醒
- 评估后续云模型接入层设计

## 下一优先级

1. 继续强化 loop 与 Codex 桌面线程的可见续发体验
2. 完善移动端查看体验与基础操作
3. 完善 Git / 文档 / 规则守护逻辑
4. 继续补长时间运行稳定性验证

## 验证要求

- `npm test`
- `npm run build:web`
- `cmd /c E:\2026\codex-loop\start-codex-loop.bat check`
- 如条件允许，再跑一次完整启动链路实机检查

## 接手前必读

1. `E:\2026\codex-loop\README.md`
2. `E:\2026\codex-loop\docs\2026-06-06-codex-loop-console-design.md`
3. `E:\2026\codex-loop\docs\template-driven-loop-setup.md`
4. 当前仓库中的 `app/server`、`app/web`、`scripts` 最新实现
