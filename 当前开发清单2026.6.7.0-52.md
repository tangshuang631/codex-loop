# 当前开发清单 2026.6.7 0:52

## 当前结论

- `codex-loop` 已从 `opencow` 独立拆出，仓库根目录为 `E:\2026\codex-loop`。
- 当前优先目标仍然是：让循环真正可启动、可停止、可持续续发，并且在前端明确看见状态、最近续发和聊天镜像。
- 当前已确认：
  - 后端核心链路可用
  - 多 loop 与线程绑定可用
  - `/api/mobile` 已接通
  - 前端已改为中文优先、白色极简、隐藏路径噪音
  - `npm run dev` 现可稳定同时拉起 API 与前端

## 本次已完成

### P0 已完成

- 打通真实线程续发链路
  - `app/server/lib/codex-dispatcher.mjs`
  - 通过本机 `codex exec resume <threadId> <prompt>` 续发到同一线程
- 打通循环控制核心接口
  - `POST /api/start`
  - `POST /api/run-turn`
  - `POST /api/stop`
  - `GET /api/health`
  - `GET /api/snapshot`
  - `GET /api/summary`
  - `GET /api/loops`
  - `GET /api/mobile`
- 修复 loop 切换后线程绑定丢失
  - 每个 loop 独立保存 `threadBinding`
  - `selectLoop` 会恢复该 loop 自己的线程绑定和上下文
- 补强健康检查
  - `heartbeat:stale`
  - `continuation:stalled`
- 完成手机只读数据导出
  - `exportMobileView()`
  - `/api/mobile`
  - 输出当前 loop、线程摘要、最近 prompt、最近 transcript
- 完成前端重构
  - 白色极简 UI
  - 中文为核心
  - 删除主界面路径噪音
  - 重要信息前置
  - 次要信息折叠
  - 手机宽度下可直接查看最近摘要、最近续发、聊天卡片
- 修复开发启动链路
  - `scripts/dev.mjs` 改为显式使用本地 `node_modules/vite/bin/vite.js`
  - 修复 Windows 环境下 `npm run dev` 可能只启动后端、不稳定拉起前端的问题
  - 当前确认一键启动后默认端口恢复为：
    - API `127.0.0.1:3000`
    - Web `127.0.0.1:3001`

### P0 已补测试

- `tests/runtime-store.test.mjs`
  - `selectLoop restores the saved thread binding for that loop`
  - `readLoopSnapshot marks health issue when heartbeat is stale`
  - `readLoopSnapshot marks health issue when continuation dispatch is stalled`
- `tests/http-server.test.mjs`
  - `handler dispatches mobile route`
- `tests/summary-export.test.mjs`
  - `exportMobileView returns recent transcript entries for mobile readers`
- `tests/loop-controller.test.mjs`
  - 重复 `start` 不会双开
  - `stop` 后不会继续调度下一轮

## 当前可用状态

- 当前工具已经具备以下闭环：
  - 开始循环
  - 立即续跑一轮
  - 停止循环
  - 查看健康状态
  - 查看每个 loop 的绑定线程
  - 查看最近续发和摘要
  - 手机端只读查看聊天镜像
- 当前前端主界面已明确显示：
  - 当前模式
  - 绑定线程
  - 已续发轮次
  - 最近事件
  - 最近写给 Codex 的消息
  - 最近 Codex 摘要
  - 手机查看区
  - 健康检查

## 仍未完成

### P0

- 还没有做到真正的桌面 GUI 级“模拟真人敲字发送”
  - 当前仍是通过 `codex exec resume` 续接同一线程
  - 线程内容可见，但不是桌面输入框层面的真实人工输入事件
- 创建 loop 后的自动线程绑定引导还不够顺滑
- 停止后统一生成最终收尾摘要的收口还不完整
- transcript 长时间不更新的健康项还可继续细化

### P1

- 手机端目前是只读查看，不支持操作
- `opencow` adapter 的验证命令仍偏通用，后续应更贴近真实项目
- 自动 checkpoint、摘要导出、长任务计划镜像仍可继续增强

## 下一优先级

1. 继续强化真实线程续发体验
   - 优化 `buildFollowupPrompt`
   - 让续发文本更像真人接话
   - 更稳定带入开发文档上下文
2. 强化 loop 创建与选择后的线程绑定体验
   - create 后给出默认线程策略
   - select 后明确显示当前绑定状态
3. 继续补健康检查
   - transcript 长时间不更新
   - 续发长期无完成
   - 关键事件卡住
4. 继续压缩 UI 噪音
   - 折叠次要信息
   - 保留关键中文状态

## 最新验证结果

- `npm test`
  - 通过，`48/48`
- `npm run build:web`
  - 通过
- 真实接口验证
  - `http://127.0.0.1:3000/api/mobile` 返回正常
  - `http://127.0.0.1:3000/api/snapshot` 返回正常
- 启动链路验证
  - `npm run dev` 已确认可稳定拉起 `3000/3001`

## 接手前必读

1. `E:\2026\codex-loop\README.md`
2. `E:\2026\codex-loop\projects\opencow\README.md`
3. `E:\2026\codex-loop\projects\opencow\AGENTS.md`
4. `E:\2026\codex-loop\projects\opencow\THREAD.md`
5. `E:\2026\opencow\OPENCOW_CORE_RULES.md`
6. `E:\2026\opencow\docs\v1.0`
7. `E:\2026\opencow\开发进度清单2026.6.6-22-48.md`
